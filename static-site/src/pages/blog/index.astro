---
import Layout from '../../layouts/Layout.astro';

// Blog posts are listed newest-first. Add new posts to the top.
const posts = [
  { slug: 'maintaining-two-apps', title: "How I maintain two apps", date: '2026-09-21', summary: "As a solo dev, figuring out how to maintain a native iOS app and a PWA was challenging, but with rust we can make it feasible." },
  { slug: 'random-words-for-claude', title: "Random Words for Claude", date: '2026-09-02', summary: "LLMs cannot choose how random to be. Every gram in the dictionary and an example sentence, as plain text in twelve languages, as a better source of inspiration than a random alphanumeric string." },
  { slug: 'unexpected-application-of-ml-theory-to-language-learning', title: "The Tokenizer and the Brain", date: '2026-03-12', summary: "How machine learning theory is finding surprising applications in language learning tools." },
];
---

<Layout title="Blog" description="Updates and thoughts on language learning, NLP, and building Yap.Town.">
  <div class="mb-6">
    <h1 class="text-3xl font-bold">Blog</h1>
  </div>

  {posts.length === 0 && (
    <p class="text-muted-foreground">No posts yet — check back soon.</p>
  )}

  <div class="space-y-6">
    {posts.map((post: any) => (
      <a href={`/blog/${post.slug}/`} class="block rounded-lg border bg-card p-4 hover:bg-secondary transition-colors no-underline">
        <div class="text-lg font-semibold text-card-foreground">{post.title}</div>
        <div class="text-sm text-muted-foreground mt-1">{post.date}</div>
        {post.summary && <p class="text-sm text-muted-foreground mt-2">{post.summary}</p>}
      </a>
    ))}
  </div>
</Layout>
